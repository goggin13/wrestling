<%= form_with(model: match, local: true) do |form| %>
  <% if match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
        <% match.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field(:weight) %>
  </div>

  <div class="field">
    <%= form.label :over_under %>
    <%= form.text_field(:over_under) %>
  </div>

  <div class="field">
    <%= form.label :spread %>
    <%= form.text_field(:spread) %>
  </div>

  <div class="field">
    <%= form.label :tournament %>
    <%= form.collection_select(:tournament_id, @tournaments, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :away_wrestler%>
    <%= form.collection_select(:away_wrestler_id, @wrestlers, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :away_score%>
    <%= form.text_field :away_score %>
  </div>

  <div class="field">
    <%= form.label :home_wrestler%>
    <%= form.collection_select(:home_wrestler_id, @wrestlers, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :home_score%>
    <%= form.text_field :home_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
